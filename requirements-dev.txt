# Development / CI tooling, pinned so a new upstream release of a linter or
# type checker can't redden CI on an unrelated change. Bump deliberately.
pytest==9.0.3
pytest-cov==7.1.0
ruff==0.15.15
mypy==2.1.0
